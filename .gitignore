# ── Dependencies ──────────────────────────────
node_modules/
.pnp
.pnp.*

# ── Builds / outputs ──────────────────────────
frontend/.next/
frontend/out/
frontend/build/
backend/dist/
*.tsbuildinfo
next-env.d.ts

# ── Environment / secrets ─────────────────────
.env
.env.*
!.env.example

# ── Local tooling / caches ────────────────────
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.mongodb-binaries/
.cache/
coverage/

# ── External reference clone (kept local only) ─
references/

# ── Agent / tooling artifacts (not part of the project) ─
.claude/
/CLAUDE.md
graphify-out/

# ── Editor ────────────────────────────────────
.idea/
.vscode/*
!.vscode/extensions.json
